#管理代码风格
- eslint
    是js风格 

- __dirname
    返回绝对的物理路径，项目未来发布到哪里都行

- .eslintrc
    采用一个业内的规范,严格执行公司的规范

#webpack配置项
- devServer
    在特定的端口启动
    获取数据

- file-loader

#
- webpack  会创建dist目录
- webpack-dev-server 编译放在内存中 为了性能提高(热更新)

yarn add webpack webpack-cli webpack-dev-server -D
yarn add babel-core babel-cli babel-preset-env -D
yarn add eslint eslint-loader -D